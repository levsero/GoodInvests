<content class="profile">
<strong class="header"> <%= user.escape("first_name") + " " + user.escape("last_name") %></strong>
<br>
<strong>Job Title:</strong> <%= user.escape("job_title") || "not yet added :(" %>
<br>
<strong>Description:</strong> <%= user.escape("description") || "not yet added :(" %>
<br>
</content>
